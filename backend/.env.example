# ━━━ Server ━━━
NODE_ENV=development
PORT=4000

# ━━━ Database ━━━
DB_CONNECTION_MODE=local
# Local MongoDB
DB_LOCAL_HOST=localhost
DB_LOCAL_PORT=27017
DB_LOCAL_NAME=new-starter
DB_LOCAL_USER=
DB_LOCAL_PASSWORD=
# Atlas MongoDB (required if DB_CONNECTION_MODE=atlas)
DATABASE_URI=
DB_ATLAS_NAME=new-starter

# ━━━ Authentication (REQUIRED) ━━━
ACCESS_TOKEN_SECRET=your-access-token-secret-here
REFRESH_TOKEN_SECRET=your-refresh-token-secret-here
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d
JWT_ISSUER=new-starter-backend-v1
JWT_AUDIENCE=new-starter-web-client

# ━━━ Email (REQUIRED for registration/password reset) ━━━
MAILTRAP_HOST=sandbox.smtp.mailtrap.io
MAILTRAP_PORT=2525
MAILTRAP_USER=your_mailtrap_username
MAILTRAP_PASS=your_mailtrap_password

# ━━━ CORS ━━━
ALLOWED_ORIGINS=http://localhost:3000

# ━━━ Redis (for email queue) ━━━
REDIS_HOST=localhost
REDIS_PORT=6379

# ━━━ Cookies ━━━
COOKIE_DOMAIN=

# ━━━ Logging ━━━
LOG_LEVEL=info
